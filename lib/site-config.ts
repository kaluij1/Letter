/** Customize the recipient's name via env or edit the default below. */
export const FRIEND_NAME =
  process.env.NEXT_PUBLIC_FRIEND_NAME?.trim() || "Jess";

export const SENDER_NAME =
  process.env.NEXT_PUBLIC_SENDER_NAME?.trim() || "Someone who cares";
